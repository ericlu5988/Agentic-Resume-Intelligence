\documentclass[11pt]{article}

% ---------------------------------------------------------
% PACKAGES & THEME
% ---------------------------------------------------------
\usepackage[margin=1in]{geometry}
\usepackage{fontspec}
\usepackage{textcomp}
\setmainfont{EB Garamond}[
  Mapping=
]
\usepackage[hidelinks]{hyperref}
\usepackage{parskip}
\usepackage{xcolor}

\definecolor{briefblue}{RGB}{0, 51, 102}

((# 
  AGENT HINT: REQUIRED JSON SCHEMA
  -------------------------------
  {
    "name": "Candidate Name",
    "location": "Candidate City, ST",
    "phone": "Candidate Phone",
    "email": "Candidate Email",
    "linkedin": "Candidate LinkedIn",
    "company": "Target Company",
    "target_location": "Target Location",
    "paragraphs": ["Para 1", "Para 2", "Para 3", "Para 4"]
  }
#))

% ---------------------------------------------------------
\begin{document}

% --- SENDER INFO ---
\begin{flushright}
    \textbf{((( resume.name|latex_escape )))}\\
    ((( resume.location|latex_escape )))\\
    ((( resume.phone|latex_escape )))\\
    ((( resume.email|latex_escape )))\\
    \href{https://((( resume.linkedin|latex_escape )))}{((( resume.linkedin|latex_escape )))}
\end{flushright}

\vspace{1em}

% --- DATE ---
\today

\vspace{1em}

% --- RECIPIENT INFO ---
\textbf{Hiring Team}\\
((( resume.company|latex_escape )))\\
((( resume.target_location|default('Company Headquarters')|latex_escape )))

\vspace{2em}

% --- SALUTATION ---
Dear Hiring Manager,

\vspace{1em}

% --- BODY ---
((% for para in resume.paragraphs %))
((( para|latex_escape )))

\vspace{1em}
((% endfor %))

\vspace{1em}

% --- CLOSING ---
Sincerely,

\vspace{3em}

((( resume.name|latex_escape )))

\end{document}
